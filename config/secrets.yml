# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 257eb259eb4c0c59423e86d3b780e4f35e41ae029dbc946c0086fcdaa98115e78072ff60a9ecfc81d133048478b2bb87b5dde4ff30983ca6d9af0ac0d4f269f7
  twilio_account_sid: <%= ENV["TWILIO_ACCOUNT_SID"] %>
  twilio_auth_token: <%= ENV["TWILIO_AUTH_TOKEN"] %>
  facebook_app_id: <%= ENV['FACEBOOK_APP_ID'] %>
  facebook_app_secret: <%= ENV['FACEBOOK_APP_SECRET'] %>

test:
  secret_key_base: ca47a2a40730677be31f59be0382290b8c5e96c3b94ac6ea31840c2f808bb75194619fe4d5a1c853aec070532875ca8a3cf76e817e32adfcbf903c1da1b5ee0b

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
